apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: book-postgres-data
  name: book-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
